\documentclass[12pt]{article}   % задает тип документа с размером шрифта 12 пунктов 
\usepackage[utf8]{inputenc}     % кодировка utf8 
\usepackage[russian]{babel}     % добавляется русский язык (кирилица) 
\usepackage{amsmath, amssymb}   % расширенные функции математики для формул 
\usepackage{geometry}           % функции геометрии 
\geometry{margin=2.5cm}         % настройка полей страницы 




\begin{document}

\setcounter{section}{10} % Устанавливает счётчик секций вручную на 10 
\section*{\S\,11 \hfill ПРОСТЕЙШИЕ ЗАДАЧИ \hfill 187} % заголовок секции без номера и раздвигание элементов по горизонтали 

\noindent     %Убирает отступ в начале абзаца
ласти. Тогда можно воспользоваться формулой Остроградского
\[
  \iint_{S} W_n \, d\sigma = \iiint_{V} \operatorname{div} \mathbf{W} \, dV
\]
и преобразовать уравнение баланса к виду
\[
  \iiint_{V} c\rho \left[ u(P, t_2) - u(P, t_1) \right] dV_P =
  - \int_{t_1}^{t_2} \iiint_{V} \operatorname{div} \mathbf{W} \, dV_P \, dt
  + \int_{t_1}^{t_2} \iiint_{V} F(P, t) \, dV_P \, dt.
\]

\noindent % сюда тоже на всякий напишу Убирает отступ в начале абзаца
(Будем предполагать $F(P,t)$ непрерывной функцией своих аргументов.)

Применяя теорему о среднем и теорему о конечных приращениях для функций многих переменных, получим:
\[
  c\rho \left. \frac{\partial u}{\partial t} \right|_{\substack{t = t_3 \\ P = P_1}}
  \Delta t \cdot V
  = -\left. \operatorname{div} \mathbf{W} \right|_{\substack{t = t_4 \\ P = P_2}}
  \Delta t \cdot V
  + \left. F \right|_{\substack{t = t_5 \\ P = P_3}}
  \Delta t \cdot V,
\]
где $t_3$, $t_4$, $t_5$ — промежуточные точки на интервале $\Delta t$, а $P_1$, $P_2$, $P_3$ — точки в объёме $V$. Фиксируем некоторую точку $M(x, y, z)$ внутри $V$ и будем стягивать $V$ в эту точку, а $\Delta t$ стремить к нулю. После сокращения на $\Delta t V$ и указанного предельного перехода получим:
\[
  c\rho \, \frac{\partial u}{\partial t}(x, y, z, t) = -\operatorname{div} \mathbf{W}(x, y, z, t) + F(x, y, z, t).
\]

\noindent % и сюда Убирает отступ в начале абзаца
Заменяя $\mathbf{W}$ по формуле $\mathbf{W} = -k\,\operatorname{grad} u$, получим дифференциальное уравнение теплопроводности
\[
  c\rho \, u_t = \operatorname{div}(k\,\operatorname{grad} u) + F
\]
или
\[
  c\rho \, u_t = \frac{\partial}{\partial x}\!\left(k\,\frac{\partial u}{\partial x}\right)
              + \frac{\partial}{\partial y}\!\left(k\,\frac{\partial u}{\partial y}\right)
              + \frac{\partial}{\partial z}\!\left(k\,\frac{\partial u}{\partial z}\right) + F.
\]

\noindent
Если среда однородна, то это уравнение обычно записывают в виде
\[
  u_t = a^2\left(u_{xx} + u_{yy} + u_{zz}\right) + \frac{F}{c\rho},
\]
где $a^2 = k/c\rho$ — коэффициент температуропроводности, или
\[
  u_t = a^2 \Delta u + f \qquad \left(f = \frac{F}{c\rho}\right),
\]
где $\Delta = \dfrac{\partial^2}{\partial x^2} + \dfrac{\partial^2}{\partial y^2} + \dfrac{\partial^2}{\partial z^2}$ — \textbf{оператор Лапласа}.

\bigskip %Большой вертикальный отступ
\hrule   % большая горизонтальная страница в всю длину 
\bigskip %Большой вертикальный отступ

\noindent %убирает отступ в начале абзаца 
\textbf{188 \quad УРАВНЕНИЯ ПАРАБОЛИЧЕСКОГО ТИПА \quad [ГЛ. III} 
% фирные текст и биольщой ГОРИЗОНТАЛЬНЫЙ ПРОБЕЛ 
\bigskip

\noindent
\textbf{4. Постановка краевых задач.} Для выделения единственного решения уравнения теплопроводности необходимо к уравнению присоединить начальные и граничные условия.

Начальное условие в отличие от уравнения гиперболического типа состоит лишь в задании значений функции $u(x,t)$ в начальный момент $t_0$.

Граничные условия могут быть различны в зависимости от температурного режима на границах. Рассматривают три основных типа граничных условий.

\medskip % 
\noindent
1. На конце стержня $x = 0$ задана температура
\[
  u(0,\, t) = \mu(t),
\]
где $\mu(t)$ — функция, заданная в некотором промежутке $t_0 \leqslant t \leqslant T$, причём $T$ есть промежуток времени, в течение которого изучается процесс.

\medskip
\noindent
2. На конце $x = l$ задано значение производной
\[
  \frac{\partial u}{\partial x}(l,\, t) = \nu(t).
\]
К этому условию мы приходим, если задана величина теплового потока $Q(l,t)$, протекающего через торцевое сечение стержня,
\[
  Q(l,\, t) = -k\,\frac{\partial u}{\partial x}(l,\, t),
\]
откуда $\dfrac{\partial u}{\partial x}(l,t) = \nu(t)$, где $\nu(t)$ — известная функция, выражающаяся через заданный поток $Q(l,t)$ по формуле
\[
  \nu(t) = -\frac{Q(l,\,t)}{k}\,.
\]

\medskip % средний вертикальный отступ между абзацами 
\noindent
3. На конце $x = l$ задано линейное соотношение между производной и функцией
\[
  \frac{\partial u}{\partial x}(l,\, t) = -\lambda\left[u(l,\, t) - \theta(t)\right].
\]
Это граничное условие соответствует теплообмену по закону Ньютона на поверхности тела с окружающей средой, температура которой $\theta$ известна. Пользуясь двумя выражениями для теплового потока, вытекающего через сечение $x = l$,
\[
  Q = h(u - \theta)
\]
и
\[
  Q = -k\,\frac{\partial u}{\partial x}
\]
получаем математическую формулировку третьего граничного условия в виде
\[
  \frac{\partial u}{\partial x}(l,\, t) = -\lambda\left[u(l,\, t) - \theta(t)\right],
\]

\end{document}



| Команда | Что делает |
|---|---|
| `\documentclass[12pt]{article}` | Задаёт класс документа — статья с размером шрифта 12pt |
| `\begin{document}` / `\end{document}` | Обозначают начало и конец содержимого документа |
| `\usepackage{...}` | Подключает пакеты расширений |

### Подключённые пакеты
| Пакет | Назначение |
|---|---|
| `[utf8]{inputenc}` | Кодировка файла — поддержка UTF-8 |
| `[russian]{babel}` | Русский язык — переносы, названия |
| `amsmath, amssymb` | Расширенные математические формулы и символы |
| `geometry` | Настройка полей страницы |

---

## Форматирование текста

| Команда | Что делает |
|---|---|
| `\noindent` | Убирает отступ в начале абзаца |
| `\textbf{...}` | **Жирный** текст |
| `\medskip` | Средний вертикальный отступ между абзацами |
| `\bigskip` | Большой вертикальный отступ |
| `\hrule` | Горизонтальная линия на всю ширину страницы |
| `\hfill` | Растягивает пространство — раздвигает элементы по горизонтали |

---

## Математические окружения

| Команда | Что делает |
|---|---|
| `\[ ... \]` | Формула на отдельной строке (выключная), без нумерации |
| `\left( ... \right)` | Автоматически масштабируемые скобки по высоте содержимого |
| `\frac{числитель}{знаменатель}` | Дробь |
| `\dfrac{...}{...}` | Дробь в полный размер, даже внутри текста |

---

## Специальные символы и команды

| Команда | Что делает |
|---|---|
| `\S` | Символ §  |
| `\quad` | Широкий горизонтальный пробел |
| `\,` | Узкий горизонтальный пробел |
| `\leqslant` | Символ ⩽ |
| `\cdot` | Точка умножения · |
| `\left|_{...}` | Вертикальная черта с подстрочным индексом (значение в точке) |

---

## Вёрстка формул

| Команда | Что делает |
|---|---|
| `\substack{...\\...}` | Несколько строк в индексе |
| `\operatorname{div}` | Оператор как текст (не курсив) внутри формулы |
| `\frac{\partial}{\partial x}` | Частная производная |
| `\iiint` / `\iint` / `\int` | Тройной, двойной и одинарный интегралы |

---

## Секции и счётчики

| Команда | Что делает |
|---|---|
| `\setcounter{section}{10}` | Устанавливает счётчик секций вручную на 10 |
| `\section*{...}` | Заголовок секции без нумерации (`*` отключает счётчик) |
| `\geometry{margin=2.5cm}` | Устанавливает поля страницы 2.5 см со всех сторон |
